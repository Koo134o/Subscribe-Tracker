-# 💳 Subscribe Tracker
> **결제 알림 분석 기반의 자동 구독 관리 플랫폼**


* **Name**: 고민경
* **Student ID**: 22421580
* **GitHub Repository**: https://github.com/Koo134o/Subscribe-Tracker

---

### [ Revision History ]

| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026/06/04 | 0.1 | 초기 버전 | 고민경 |
| 2026/06/05 | 0.2 | add class diagram | 고민경 |

---

## 📑 Contents
1.[ Introduction](#1-introduction)
2.[ Class Diagram](#2-class-diagram)
3.[ Sequence Diagram](#3-sequence-diagram)
4.[ State Machine Diagram](#4-state-machine-diagram)
5.[ Implementation Requirements](#5-implementation-requirements)
6.[ Glossary](#6-glossary)
7.[ References](#7-references)

---

## 🌸 1. Introduction

현대 사회는 다양한 산업에 걸쳐 유료 구독 서비스가 급부상하고 있으나, 결제 시점과 수단이 여러 플랫폼에 파편화되어 있어 소비자가 매달 지출되는 고정 비용의 규모를 한눈에 파악하기 어렵다. 
또한 무료 체험 기간이 종료된 후 해지 타이밍을 놓치거나 자주 사용하지 않는 서비스를 방치하여 불필요한 자동 결제 지출이 발생하는 문제도 빈번하게 일어난다.
본 프로젝트 'Subscribe Tracker'는 이러한 일상적인 불편함을 해결하기 위해 기획되었으며, 사용자가 수신한 결제 알림 문자 메시지나 이메일 텍스트 데이터를 자동으로 분석하여 구독 정보를 간편하게 등록해 주는 스마트 구독 관리 플랫폼을 제안한다.
본 보고서는 지난 요구사항 Analysis phase에서 도출된 세부 기능들과 유스케이스 정의를 바탕으로 시스템을 구현하기 위해 필요한 구체적인 아키텍처와 설계 구조를 기술한 Design Document이다.

---

## 🌸 2. Class diagram
### 🔹 2.1 Diagram
<img width="539" height="580" alt="class" src="https://github.com/user-attachments/assets/968390df-91fb-42d0-9990-0dfecc7451bc" />  

### 🔹 2.2 Detailed Class Specifications

#### 2.2.1 User Class (사용자 엔티티)
* **Description:** 플랫폼에 가입된 개별 사용자의 계정 로그인 자격 증명 및 이메일/푸시 알림 수신 동의 상태 설정을 내포하여 관리하는 도메인 핵심 엔티티 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Attribute** | `private (-)` | `email` | `String` | 사용자의 고유 식별값(ID)으로 활용되는 이메일 주소 |
| **Attribute** | `private (-)` | `password` | `String` | 암호화되어 데이터베이스 계정 레코드에 저장되는 비밀번호 |
| **Attribute** | `private (-)` | `isPaymentAlertOn` | `Boolean` | 설정 화면 내 '결제일 사전 알림' 토글스위치 활성화 동의 여부 플래그 |
| **Attribute** | `private (-)` | `isMonthlyReportOn` | `Boolean` | 설정 화면 내 '월간 지출 요약 알림' 토글스위치 활성화 동의 여부 플래그 |

#### 2.2.2 AuthController Class (인증 제어 컨트롤러)
* **Description:** 로그인, 회원가입, 생체인증 등록 요청의 프론트엔드 이벤트를 수신하여 사용자 인증 세션과 보안 제어 흐름을 관장하는 컨트롤러 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `signUp(User user)` | `Boolean` | 이메일과 비밀번호 기반으로 구성된 유저 객체를 검증하고 가입을 최종 승인 처리함 |
| **Method** | `public (+)` | `login(String email, String password)` | `Boolean` | 화면 입력 자격 증명을 가용자 레코드와 비교 대조하여 정상 세션 로그인을 수행함 |
| **Method** | `public (+)` | `loginWithBiometric(String email)` | `Boolean` | 생체인증 로그인 UI 트리거 시 디바이스 지문 신호를 통해 퀵 로그인을 처리함 |
| **Method** | `public (+)` | `registerBiometric(String email)` | `Boolean` | 지문/Face ID 연동 화면에서 가용자의 디바이스 고유 생체 인증 수단을 최종 등록 및 매핑함 |
| **Method** | `public (+)` | `logout(String email)` | `Boolean` | 현재 로그인된 사용자의 활성화 세션을 만료하고 초기 로그인 화면으로 복귀함 |

#### 2.2.3 UserRepository Interface (사용자 데이터 영속성 인터페이스)
* **Description:** 가용 사용자 객체(`User`) 데이터의 데이터베이스 삽입 저장을 전담하는 영속성 데이터 액세스 인터페이스 계층이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `save(User user)` | `void` | 신규 유저 등록 레코드 및 변경된 알림 플래그 설정 값을 데이터베이스에 영구 반영함 |

#### 2.2.4 Subscription Class (구독 정보 엔티티)
* **Description:** 가용자가 등록하여 통합 추적하는 개별 고정 정기 결제 서비스의 세부 결제 비용 액수와 정형 메타데이터를 보유한 엔티티 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Attribute** | `private (-)` | `id` | `Long` | 개별 구독 레코드 식별을 위한 인덱스 일련번호 (데이터베이스 PK) |
| **Attribute** | `private (-)` | `userEmail` | `String` | 해당 구독 정보를 소유하고 귀속 관리하는 사용자 계정 외래키 (FK) |
| **Attribute** | `private (-)` | `serviceName` | `String` | 정기 결제가 발생하는 구독 마켓 서비스 명칭 (예: Netflix) |
| **Attribute** | `private (-)` | `price` | `Double` | 정기 결제 주기마다 청구되는 원금 결제 수치 |
| **Attribute** | `private (-)` | `currency` | `String` | 결제 알림 문자 내 파싱된 통화 기호 코드 (예: KRW, USD) |
| **Attribute** | `private (-)` | `paymentDate` | `Date` | 매달 반복적으로 자동 결제가 약정 실행되는 기준 일자 정보 |
| **Attribute** | `private (-)` | `memo` | `String` | 사용자가 상세조회 화면에서 개별 목적으로 기입해 둔 추가 텍스트 내용 |
| **Attribute** | `private (-)` | `category` | `String` | 시각화 통계 처리를 위해 시스템 내부 규칙으로 자동 매핑된 분류 (예: OTT) |
| **Method** | `public (+)` | `getConvertedPrice(Double baseRate)` | `Double` | 외화 단위 수치에 REST API 수신 환율을 연산하여 정밀 원화(KRW) 금액을 도출함 |

#### 2.2.5 SubscriptionController Class (구독 제어 컨트롤러)
* **Description:** 메인 대시보드, 상세조회, 구독 추가 화면의 이벤트를 통제하여 구독 데이터의 생성, 조회, 수정, 삭제(CRUD) 제어 로직을 통합 관리하는 아키텍처 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `addManualSubscription(Subscription subData)` | `Boolean` | 가용자가 폼 양식에 수동 입력한 신규 구독 명세 엔티티 객체를 수신해 영속 계층에 보관함 |
| **Method** | `public (+)` | `addAutoSubscription(String rawText, String email)` | `Boolean` | 입력된 결제 알림 텍스트를 파싱 유틸에 위임한 후 정제된 객체를 최종 저장 처리함 |
| **Method** | `public (+)` | `modifySubscriptionDetails(Subscription modifiedData)` | `Boolean` | 상세 내역 화면에서 사용자가 변경 수정한 필드 값들을 데이터베이스 컨텍스트에 갱신함 |
| **Method** | `public (+)` | `deleteSubscription(Long subId)` | `Boolean` | 특정 대시보드 일련번호 항목을 명시적으로 파괴하여 영속 데이터베이스에서 영구 제외함 |
| **Method** | `public (+)` | `getDashboardSubscriptions(String email)` | `List<Subscription>` | 메인 대시보드 화면 렌더링 시 현재 로그인 세션 사용자가 보유한 전체 구독 리스트를 리턴함 |

#### 2.2.6 SubscriptionRepository Interface (구독 데이터 영속성 인터페이스)
* **Description:** 구독 엔티티 데이터 레코드에 대한 CRUD 실제 데이터베이스 로우레벨 액세스를 전담 수행하는 인터페이스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `save(Subscription sub)` | `void` | 신규 또는 수정 내역이 반영된 구독 엔티티 레코드를 영속 컨텍스트 데이터베이스에 반영함 |
| **Method** | `public (+)` | `deleteById(Long subId)` | `void` | 전달받은 고유 일련번호 고유 PK 레코드를 데이터베이스 테이블 내에서 영구 파기함 |
| **Method** | `public (+)` | `findAllByUserEmail(String email)` | `List<Subscription>` | 특정 가용자 계정 이메일(FK)에 귀속 등록된 모든 구독 로우 데이터를 수집해 컬렉션으로 리턴함 |

#### 2.2.7 TextAnalysisEngine Class (비정형 텍스트 분석 엔진)
* **Description:** 결제 알림창 문자열 인풋 내부에서 정규표현식 매칭 패턴 규칙을 전개하여 필요한 도메인 정형 정보만을 정밀 분리해 내는 유틸리티 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `parseNotificationText(String rawText)` | `ParsedSubDataDTO` | 문자열 패턴 내에서 정규식 컴파일 매칭을 구동해 가맹점명, 비용, 통화, 약정일을 추출 후 DTO 객체로 리턴함 |

#### 2.2.8 ParsedSubDataDTO Class (파싱 데이터 전송 객체)
* **Description:** 비정형 텍스트 분석기 결과값으로 가공 도출된 정형 파싱 핵심 필드들을 레이어 간 안전하게 실어 나르기 위한 경량 전송용 클래스이다. 모든 필드 멤버 변수는 객체 제어 장벽을 우회해 신속히 수송하기 위해 public 접근자를 가진다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Attribute** | `public (+)` | `serviceName` | `String` | 텍스트 내 패턴 추출이 완료된 원 가맹점 서비스 명칭 데이터 |
| **Attribute** | `public (+)` | `price` | `Double` | 알림 문자 내 통계화가 가능한 숫자 형태로 정제 추출된 결제 대금 수치 |
| **Attribute** | `public (+)` | `currency` | `String` | 원금 대금의 기준이 되는 통화 규격 코드 기호 문자열 |
| **Attribute** | `public (+)` | `paymentDate` | `Date` | 텍스트 내에서 파싱 연산되어 명세화된 월 정기 자동 결제 기준 일자 |

#### 2.2.9 ExchangeRateService Class (외부 API 연동 서비스)
* **Description:** 외부 공공 기관 환율 정보 제공 OPEN API 플랫폼과의 REST 통신 커넥션을 안정적으로 수립하여 실시간 기준 환율 데이터를 메모리에 실시간 동기화하는 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Attribute** | `private (-)` | `baseRate` | `Double` | REST API 통신 응답을 가공하여 캐싱해 둔 1달러(USD) 당 원화(KRW) 변환율 수치 |
| **Method** | `public (+)` | `fetchCurrentRate(String currency)` | `Double` | 외화 코드를 인풋 파라미터로 받아 외부 환율 정보 통신을 체결 후 `baseRate` 필드를 최신화하여 반환함 |

#### 2.2.10 AnalyticsService Class (비즈니스 통계 분석 서비스)
* **Description:** 메인 대시보드 카드 지표 정보 및 소비 분석 리포트 화면 내 주황색 막대그래프 컴포넌트에 실시간 주입될 데이터 통계 처리를 총괄하는 비즈니스 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `generateSpendingReport(String email)` | `SpendingReportDTO` | 타겟 가용자의 전체 목록을 로드하여 환율 총합 계산, 카테고리 비중 연산, 전월 편차 도출을 통합 계산해 결과 DTO를 생성함 |

#### 2.2.11 SpendingReportDTO Class (소비 리포트 데이터 전송 객체)
* **Description:** 분석 로직이 집계 완료한 총액 지표, 그래프용 비중 데이터 구조 맵, 전월 대비 편차 값을 묶어 뷰 컴포넌트로 전달하기 위한 데이터 구조체 클래스이다. 모든 필드 멤버 변수는 객체 제어 장벽을 우회해 신속히 수송하기 위해 public 접근자를 가진다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Attribute** | `public (+)` | `totalSpending` | `Double` | 당월에 발생한 모든 구독 비용들의 원화 환산 기준 최종 누적 총지출 합산액 |
| **Attribute** | `public (+)` | `categoryDistribution` | `Map` | 주황색 바 차트 레이아웃 드로잉에 매핑될 카테고리 속성별 누적 지출액 통계 맵 |
| **Attribute** | `public (+)` | `compareDelta` | `Double` | 지난달 누적 결제액 데이터 레코드와의 가감 차액 비교 편차 수치 정보 |

#### 2.2.12 NotificationService Class (백그라운드 알림 서비스)
* **Description:** 시스템 백그라운드 스케줄러 스레드와 연동되어 사용자의 개별 알림 수신 설정 플래그 변수 상태에 따라 정기 결제 푸시 알림 및 메일링 패킷 송신을 제어하는 클래스이다.

| Element Type | Visibility | Name / Signature | Type / Return Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Method** | `public (+)` | `sendPushNotification(String email, Long subId)` | `void` | 시스템 백그라운드 스레드가 구독 객체의 결제일 3일 전 도래를 감지하는 순간 사용자 기기로 푸시 패킷을 유선 전송함 |
| **Method** | `public (+)` | `sendMonthlyEmailReport(String email, SpendingReportDTO report)` | `void` | 유저 엔티티 내 `isMonthlyReportOn`이 true 상태일 시, 빌드된 통계 리포트 텍스트를 사용자 메일 주소로 발송함 |
| **Method** | `public (+)` | `updateNotificationToggle(String email, String settingType)` | `Boolean` | 환경 설정 화면에서 유저가 주황색 토글스위치를 변경할 때 유저 객체의 해당 플래그 필드를 반전 갱신하고 최종 값을 리턴함 |

---

## 🌸 3.  Sequence diagram

---

## 🌸 4.  State machine diagram

---

## 🌸 5.  Implementation requirements

---

## 🌸 6. Glossary

--- 

## 🌸 7. References
